export type Screen = "today" | "study" | "library" | "editor" | "settings";
export type ThemeMode = "system" | "light" | "dark";

export const screens: Screen[] = [
  "today",
  "study",
  "library",
  "editor",
  "settings",
];
